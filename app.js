const $=s=>document.querySelector(s);
const SUPABASE_URL='https://ghaxikrkosdeelqhfice.supabase.co',KEY='sb_publishable_N_vKWdSfxtnZVGsStl2YGQ_uxdoU3Wb';
const VAPID_PUBLIC_KEY='BCJXZ0kIKp0-tT4z12oIt3uhEjTcjR9YmF8IbcTQsPMKcaWXD4Tj--mESKXyZJyirqh521q8f9_enNlBVVIRxyU';
const db=supabase.createClient(SUPABASE_URL,KEY),names=['KD','KP','KF','KR','KT','KA','KW','KK','KC','KO','KB'];
const DEMO_MODE=false,IS_RECOVERY=location.hash.includes('type=recovery')||location.search.includes('type=recovery');
const freshLine=name=>({name,stage:'idle',color:'',bottle:'',productionUnit:'pieces',productionTarget:0,productionDone:0,handoverShift:'',handoverNote:'',handoverBy:'',handoverAt:'',prePieces:10000,bag1:1,bag2:1,extraRequested:false,extraLoaded:false,extraAssignedTo:'',assignedTo:'',lineOwnerId:'',lineOwnerName:'',remainingPieces:null,ctDecision:'',cleanConfirmed:false,nextBottle:'',activity:[]});
const initial=()=>({bottles:[],bottleColors:{},bottleNames:{},bottleImages:{},bottleLines:{},lines:Object.fromEntries(names.map(n=>[n,freshLine(n)]))});
let session=null,profile=null,appState=initial(),selected='KD',saving=false,loading=false,lastRemoteUpdated='',archiveScope='line',pendingAudit=[],liveChannel=null,historyLoading=false,operators=[];
const labels={idle:'Nessuna richiesta',running:'In corso',pre:'Pre-raclage',progress:'Pre-raclage','waiting-raclage':'Pre-raclage',raclage:'Raclage','waiting-ct':'Raclage','raclage-ok':'Raclage OK',unload:'Scarico linea',finished:'Finito'};
const cls=s=>['running'].includes(s)?'progress':['pre','progress','waiting-raclage'].includes(s)?'pre':s==='unload'?'unload':s==='finished'?'finished':['raclage','waiting-ct','raclage-ok'].includes(s)?'raclage':'';
const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const now=()=>new Date().toISOString(),showTime=i=>new Date(i).toLocaleString('it-IT',{hour:'2-digit',minute:'2-digit',day:'2-digit',month:'2-digit'});
const actor=()=>profile?.display_name||profile?.email||'Utente',isCT=()=>['ct','admin'].includes(profile?.role),isAdmin=()=>profile?.role==='admin';
const canOperateLine=l=>isCT()||!l.lineOwnerId||l.lineOwnerId===profile?.id;
const unitLabel=l=>l.productionUnit==='ot'?'O.T.':'pezzi';
const productionRemaining=l=>Math.max(0,Number(l.productionTarget||0)-Number(l.productionDone||0));
function toast(t,e=false){const x=$('#toast');x.textContent=t;x.style.background=e?'#991b1b':'#172033';x.style.display='block';setTimeout(()=>x.style.display='none',2600)}
function addBottleToLine(code,line=selected){appState.bottleLines[code]=appState.bottleLines[code]||[];if(!appState.bottleLines[code].includes(line))appState.bottleLines[code].push(line)}
function archiveCodes(){return archiveScope==='all'?appState.bottles:appState.bottles.filter(b=>(appState.bottleLines[b]||[]).includes(selected))}
function setArchiveScope(scope){archiveScope=scope;render()}
function activityKind(l){if(['raclage','waiting-ct','raclage-ok'].includes(l.stage)||(l.stage==='progress'&&l.remainingPieces===-1))return'raclage';if(['pre','progress','waiting-raclage'].includes(l.stage))return'pre';if(l.stage==='running')return'progress';if(l.stage==='unload')return'unload';if(l.stage==='finished')return'finished';return''}
function displayActivityKind(e){const t=(e.text||'').toLowerCase();if(t.includes('scarico'))return'unload';if(t.includes('linea completamente pulita')||t.includes('linea pulita'))return'finished';if(t.includes('raclage chiamato')||t.includes('verifica raclage')||t.includes('pezzi avanzati')||t.includes('risposta ct')||t.includes('stato corretto manualmente: raclage'))return'raclage';if(t.includes('pre-raclage')||t.includes('disponibilità')||t.includes('big bag'))return'pre';if(t.includes('produzione iniziata')||t.includes('inizio carico')||t.includes('codice cambiato')||t.includes('stato corretto manualmente: in corso'))return'progress';return e.kind||''}
function event(l,text){const entry={id:crypto.randomUUID(),line:l.name,text,actor:actor(),at:now(),kind:activityKind(l)};l.activity.unshift(entry);l.activity=l.activity.slice(0,100);pendingAudit.push(entry)}
function notificationAllowed(){return 'Notification'in window&&Notification.permission==='granted'}
function urlBase64ToUint8Array(value){const pad='='.repeat((4-value.length%4)%4),base64=(value+pad).replace(/-/g,'+').replace(/_/g,'/'),raw=atob(base64);return Uint8Array.from([...raw].map(c=>c.charCodeAt(0)))}
async function enableNotifications(){try{if(!('serviceWorker'in navigator)||!('PushManager'in window)||!('Notification'in window))throw Error('Installa l’app sulla schermata Home per usare le notifiche push');const result=await Notification.requestPermission();if(result!=='granted')throw Error('Notifiche non autorizzate nelle impostazioni del telefono');const registration=await navigator.serviceWorker.ready;let subscription=await registration.pushManager.getSubscription();if(!subscription)subscription=await registration.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:urlBase64ToUint8Array(VAPID_PUBLIC_KEY)});const json=subscription.toJSON(),{error}=await db.from('push_subscriptions').upsert({user_id:session.user.id,endpoint:json.endpoint,p256dh:json.keys.p256dh,auth:json.keys.auth,user_agent:navigator.userAgent},{onConflict:'endpoint'});if(error)throw error;toast('Notifiche push attivate');render()}catch(e){toast(e.message||'Impossibile attivare le notifiche',true)}}
async function sendPushEvents(events){if(!events.length)return;const {error}=await db.functions.invoke('send-push',{body:{events:events.map(e=>({line:e.line,text:e.text,kind:e.kind}))}});if(error)console.warn('Push non inviato',error)}
function notifyRemote(line,text){toast(`Linea ${line}: ${text}`);if(notificationAllowed()&&document.hidden)new Notification(`Reparto Live · Linea ${line}`,{body:text,icon:'./icon.svg',tag:`line-${line}`})}
function applyRemote(row){if(!row||row.updated_at===lastRemoteUpdated||saving)return;const previous=appState;appState=normalize(row.payload||initial());lastRemoteUpdated=row.updated_at;for(const n of names){const before=previous.lines?.[n]?.activity?.[0],after=appState.lines[n]?.activity?.[0];if(after&&after.id!==before?.id&&after.actor!==actor())notifyRemote(n,after.text)}render();setSyncStatus('Sincronizzato in tempo reale')}
function normalize(s){s.bottles=(s.bottles||[]).map(x=>String(x).trim());s.bottleColors=s.bottleColors||{};s.bottleNames=s.bottleNames||{};s.bottleImages=s.bottleImages||{};const hadBottleLines=!!s.bottleLines;s.bottleLines=s.bottleLines||{};for(const b of s.bottles)if(!s.bottleLines[b])s.bottleLines[b]=hadBottleLines?[]:[...names];for(const n of names){s.lines[n]={...freshLine(n),...(s.lines?.[n]||{})};s.lines[n].bottle=String(s.lines[n].bottle||'').trim()}return s}
async function signIn(){const email=$('#loginEmail').value.trim(),password=$('#loginPassword').value;if(!email||!password)return toast('Inserisci e-mail e password',true);const {error}=await db.auth.signInWithPassword({email,password});if(error)return toast('E-mail o password non corretti',true)}
async function signUp(){const first=$('#regFirst').value.trim(),last=$('#regLast').value.trim(),username=$('#regUsername').value.trim(),email=$('#regEmail').value.trim(),password=$('#regPassword').value,confirmPassword=$('#regPasswordConfirm').value;if(!first||!last||!username||!email||password.length<8)return toast('Compila tutto e usa almeno 8 caratteri per la password',true);if(password!==confirmPassword)return toast('Le due password non sono uguali',true);const {error}=await db.auth.signUp({email,password,options:{emailRedirectTo:location.origin+location.pathname,data:{display_name:`${first} ${last}`,username}}});if(error)return toast(error.message,true);$('#access').innerHTML='<h2>Controlla la tua e-mail</h2><p>Conferma l’indirizzo. Dopo la conferma, la richiesta arriverà all’Amministratore supremo.</p>'}
async function resetPassword(){const email=($('#loginEmail')?.value||$('#resetEmail')?.value||'').trim();if(!email)return toast('Inserisci la tua e-mail',true);const {error}=await db.auth.resetPasswordForEmail(email,{redirectTo:location.origin+location.pathname+'?type=recovery'});if(error)return toast(error.message,true);toast('E-mail per creare una nuova password inviata')}
async function updatePassword(){const password=$('#newPassword').value,confirmPassword=$('#newPasswordConfirm').value;if(password.length<8)return toast('Usa almeno 8 caratteri',true);if(password!==confirmPassword)return toast('Le due password non sono uguali',true);const {error}=await db.auth.updateUser({password});if(error)return toast(error.message,true);history.replaceState({},'',location.pathname);toast('Password salvata');location.reload()}
function showPasswordChange(){const box=$('#passwordPanel');box.hidden=false;$('#insidePassword').focus()}
async function changePasswordInside(){const password=$('#insidePassword').value,confirmPassword=$('#insidePasswordConfirm').value;if(password.length<8)return toast('Usa almeno 8 caratteri',true);if(password!==confirmPassword)return toast('Le due password non sono uguali',true);const {error}=await db.auth.updateUser({password});if(error)return toast(error.message,true);$('#passwordPanel').hidden=true;$('#insidePassword').value='';$('#insidePasswordConfirm').value='';toast('Password creata correttamente')}
async function signOut(){await db.auth.signOut();location.reload()}
async function getProfile(){if(!session)return null;const {data,error}=await db.from('profiles').select('*').eq('id',session.user.id).single();if(error)throw error;return data}
function accessScreen(){
 $('#app').hidden=true;$('#identity').innerHTML='';
 if(session&&IS_RECOVERY){$('#access').innerHTML='<h2>Crea nuova password</h2><label class="field">Nuova password<input id="newPassword" type="password" minlength="8"></label><label class="field">Ripeti nuova password<input id="newPasswordConfirm" type="password" minlength="8"></label><button class="primary" id="savePassword">Salva password</button>';$('#savePassword').onclick=updatePassword;return}
 if(!session){$('#access').innerHTML='<h2>Accesso Reparto Live</h2><label class="field">E-mail<input id="loginEmail" type="email" autocomplete="email"></label><label class="field">Password<input id="loginPassword" type="password" autocomplete="current-password"></label><div class="actions"><button class="primary" id="login">Entra</button><button id="forgot">Crea/Reimposta password</button></div><hr><h3>Nuova richiesta di accesso</h3><div class="grid"><label class="field">Nome<input id="regFirst" autocomplete="given-name"></label><label class="field">Cognome<input id="regLast" autocomplete="family-name"></label><label class="field">Username<input id="regUsername" autocomplete="username"></label><label class="field">E-mail<input id="regEmail" type="email" autocomplete="email"></label><label class="field">Password<input id="regPassword" type="password" minlength="8" autocomplete="new-password"></label><label class="field">Ripeti password<input id="regPasswordConfirm" type="password" minlength="8" autocomplete="new-password"></label></div><div class="actions"><button id="register">Invia richiesta</button></div>';$('#login').onclick=signIn;$('#forgot').onclick=resetPassword;$('#register').onclick=signUp;return}
 const blocked=profile?.status==='blocked';$('#access').innerHTML=`<h2>${blocked?'Accesso bloccato':'In attesa di approvazione'}</h2><p>${blocked?'L’amministratore ha bloccato questo accesso. Non puoi rientrare senza una nuova approvazione.':'La richiesta è arrivata. L’amministratore deve approvarla prima di mostrare i dati del reparto.'}</p><button id="logout">Esci</button>`;$('#logout').onclick=signOut
}
async function save(){saving=true;try{const stamp=now(),audit=[...pendingAudit];const {error}=await db.from('shared_state').update({payload:appState,updated_at:stamp}).eq('id',1);if(error)throw error;lastRemoteUpdated=stamp;if(audit.length){const {error:auditError}=await db.from('activity_log').upsert(audit.map(e=>({id:e.id,line:e.line,event_text:e.text,actor_name:e.actor,event_kind:e.kind,created_at:e.at,user_id:session?.user?.id})));if(auditError)throw auditError;pendingAudit=pendingAudit.filter(e=>!audit.some(a=>a.id===e.id));sendPushEvents(audit)}setSyncStatus('Sincronizzato in tempo reale')}catch(e){setSyncStatus('Da sincronizzare',true);toast(e.message,true)}finally{saving=false}}
async function loadRemote(){if(saving||loading||profile?.status!=='approved')return;loading=true;try{const {data,error}=await db.from('shared_state').select('payload,updated_at').eq('id',1).single();if(error)throw error;if(data.updated_at!==lastRemoteUpdated)applyRemote(data);setSyncStatus('Sincronizzato in tempo reale')}catch(e){setSyncStatus('Connessione assente',true)}finally{loading=false}}
function setSyncStatus(text,error=false){const el=$('#syncStatus');if(el){el.textContent=text;el.style.color=error?'#b91c1c':'#166534'}}
async function loadHistory(){if(profile?.status!=='approved')return;const line=selected;historyLoading=true;try{const {data,error}=await db.from('activity_log').select('*').eq('line',line).order('created_at',{ascending:false}).limit(100);if(error)throw error;const box=$('#activity');if(box&&line===selected)box.innerHTML=data.length?data.map(e=>`<div class="event ${esc(e.event_kind||'')}"><strong>${esc(e.event_text)}</strong><small>${esc(e.actor_name)} · ${showTime(e.created_at)}</small></div>`).join(''):'<p class="muted">Nessuna attività.</p>'}catch(e){/* Lo storico locale resta visibile come riserva. */}finally{historyLoading=false}}
async function loadOperators(){if(!isCT())return;const {data,error}=await db.from('profiles').select('id,display_name,role,status').eq('role','operator').eq('status','approved').order('display_name');if(!error)operators=data||[]}
async function exportHistory(){try{toast('Preparazione esportazione…');const {data,error}=await db.from('activity_log').select('*').order('created_at',{ascending:false}).limit(10000);if(error)throw error;const q=v=>`"${String(v??'').replace(/"/g,'""')}"`,rows=[['Data e ora','Linea','Operazione','Operatore','Tipo'],...(data||[]).map(e=>[new Date(e.created_at).toLocaleString('it-IT'),e.line,e.event_text,e.actor_name,e.event_kind])],csv='\ufeff'+rows.map(r=>r.map(q).join(';')).join('\r\n'),url=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'})),a=document.createElement('a');a.href=url;a.download=`reparto-live-storico-${new Date().toISOString().slice(0,10)}.csv`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);toast(`${data.length} attività esportate`)}catch(e){toast(e.message||'Esportazione non riuscita',true)}}
function startRealtime(){if(liveChannel)db.removeChannel(liveChannel);liveChannel=db.channel('reparto-live-state').on('postgres_changes',{event:'UPDATE',schema:'public',table:'shared_state',filter:'id=eq.1'},payload=>applyRemote(payload.new)).subscribe(status=>setSyncStatus(status==='SUBSCRIBED'?'Sincronizzato in tempo reale':'Connessione in corso…',status==='CHANNEL_ERROR'))}
async function act(type,x={}){let l=appState.lines[selected];try{
 if(type==='undo'){if(!l.undo)throw Error('Non c’è un’azione da annullare');const before=l.undo;appState.lines[selected]={...before,undo:null};l=appState.lines[selected];event(l,'Ultima azione annullata');await save();render();return}
 l.undo=JSON.parse(JSON.stringify({...l,undo:null}));
 if(['start-code','change-code','save-pre','call-raclage','ct-decision','set-stage','reset','assign-line','set-production-plan'].includes(type)&&!isCT())throw Error('Questa azione è riservata al CT');
 if(!isCT()&&!canOperateLine(l))throw Error(`La linea ${selected} è affidata a ${l.lineOwnerName}`);
 if(type==='assign-line'){const owner=operators.find(o=>o.id===x.ownerId);l.lineOwnerId=owner?.id||'';l.lineOwnerName=owner?.display_name||'';event(l,owner?`Linea affidata a ${owner.display_name}`:'Assegnazione operatore rimossa')}
 else if(type==='set-production-plan'){const target=Math.max(0,Number(x.target||0));if(!target)throw Error('Inserisci l’obiettivo');if(target<l.productionDone)throw Error('L’obiettivo non può essere inferiore alla quantità già raggiunta');l.productionUnit=x.unit==='ot'?'ot':'pieces';l.productionTarget=target;event(l,`Obiettivo aggiornato: ${target.toLocaleString('it-IT')} ${unitLabel(l)}`)}
 else if(type==='set-stage'){l.stage=x.stage;event(l,`Stato corretto manualmente: ${labels[x.stage]}`)}
 else if(type==='start-code'){l.bottle=x.bottle.trim();l.productionTarget=Math.max(0,Number(x.target||0));if(!l.bottle||!appState.bottles.includes(l.bottle))throw Error('Scegli un flacone dall’archivio');if(!l.productionTarget)throw Error('Inserisci l’obiettivo di produzione');l.productionUnit=x.unit==='ot'?'ot':'pieces';l.productionDone=0;l.handoverNote='';addBottleToLine(l.bottle);l.bottleName=appState.bottleNames[l.bottle]||'Flacone';l.bottleImage=appState.bottleImages[l.bottle]||'';l.stage='running';event(l,`Produzione iniziata: ${l.bottleName} · codice ${l.bottle} · obiettivo ${l.productionTarget.toLocaleString('it-IT')} ${unitLabel(l)}`)}
 else if(type==='change-code'){const old=l.bottle;l.bottle=x.bottle.trim();l.productionTarget=Math.max(0,Number(x.target||0));if(!l.bottle||!appState.bottles.includes(l.bottle)){l.bottle=old;throw Error('Scegli un flacone dall’archivio')}if(!l.productionTarget)throw Error('Inserisci il nuovo obiettivo');l.productionUnit=x.unit==='ot'?'ot':'pieces';l.productionDone=0;l.handoverNote='';addBottleToLine(l.bottle);l.bottleName=appState.bottleNames[l.bottle]||'Flacone';l.bottleImage=appState.bottleImages[l.bottle]||'';event(l,`Codice cambiato da ${old||'—'} a ${l.bottle} · ${l.bottleName} · obiettivo ${l.productionTarget.toLocaleString('it-IT')} ${unitLabel(l)}`)}
 else if(type==='update-production'){const value=Math.max(0,Number(x.value||0));if(!l.productionTarget)throw Error('Obiettivo non impostato');if(value>l.productionTarget)throw Error(`Non puoi superare ${l.productionTarget.toLocaleString('it-IT')} ${unitLabel(l)}`);l.productionDone=value;event(l,value===l.productionTarget?`Obiettivo raggiunto: ${value.toLocaleString('it-IT')} ${unitLabel(l)}`:`Avanzamento produzione: ${value.toLocaleString('it-IT')} di ${l.productionTarget.toLocaleString('it-IT')} ${unitLabel(l)}`)}
 else if(type==='handover'){l.handoverShift=x.shift;l.handoverNote=x.note.trim();l.handoverBy=actor();l.handoverAt=now();event(l,`Passaggio consegne ${x.shift}: ${l.productionDone.toLocaleString('it-IT')}/${l.productionTarget.toLocaleString('it-IT')} ${unitLabel(l)}${l.handoverNote?` · ${l.handoverNote}`:''}`)}
 else if(type==='save-pre'){l.bottle=x.bottle.trim();if(!l.bottle)throw Error('Inserisci il codice flacone');l.prePieces=Math.max(0,Number(x.prePieces||0));l.extraRequested=x.extraRequested;l.extraLoaded=false;l.extraAssignedTo='';l.assignedTo='';l.stage='pre';event(l,`Pre-raclage chiamato: ${l.prePieces.toLocaleString('it-IT')} flaconi mancanti`)}
 else if(type==='take-pre'){l.assignedTo=actor();l.stage='progress';event(l,'Pre-raclage preso in carico')}
 else if(type==='take-extra'){if(l.extraAssignedTo)throw Error('Big bag già preso in carico');l.extraAssignedTo=actor();event(l,'Big bag aggiuntivo preso in carico')}
 else if(type==='extra-loaded'){if(l.extraAssignedTo!==actor()&&!isAdmin())throw Error('Solo chi lo ha preso in carico può confermarlo');l.extraLoaded=true;event(l,'Big bag aggiuntivo caricato')}
 else if(type==='submit-pre'){l.bag1=Number(x.bag1);l.bag2=Number(x.bag2);if(l.extraRequested&&!l.extraLoaded)throw Error('Il big bag aggiuntivo non risulta caricato');l.stage='waiting-raclage';event(l,`Disponibilità: ${l.bag1+l.bag2} big bag`)}
 else if(type==='call-raclage'){l.stage='raclage';l.assignedTo='';event(l,'Raclage chiamato')}
 else if(type==='take-raclage'){l.stage='progress';l.assignedTo=actor();l.remainingPieces=-1;event(l,'Verifica raclage presa in carico')}
 else if(type==='submit-raclage'){l.remainingPieces=Math.max(0,Number(x.remainingPieces||0));l.stage='waiting-ct';event(l,`${l.remainingPieces.toLocaleString('it-IT')} pezzi avanzati`)}
 else if(type==='ct-decision'){l.ctDecision=x.decision;l.stage=x.decision==='ok'?'raclage-ok':'raclage';event(l,`Risposta CT: ${x.decision==='ok'?'OK':'Non OK'}`)}
 else if(type==='start-unload'){l.stage='unload';l.assignedTo=actor();event(l,'Scarico linea in corso')}
 else if(type==='confirm-clean'){l.cleanConfirmed=true;l.stage='finished';event(l,'Linea completamente pulita')}
 else if(type==='next-load'){l.nextBottle=x.nextBottle.trim();l.productionTarget=Math.max(0,Number(x.target||0));if(!l.nextBottle||!appState.bottles.includes(l.nextBottle))throw Error('Il codice successivo non è presente nell’archivio');if(!l.productionTarget)throw Error('Inserisci l’obiettivo del nuovo codice');l.productionUnit=x.unit==='ot'?'ot':'pieces';l.productionDone=0;l.handoverNote='';addBottleToLine(l.nextBottle);l.bottle=l.nextBottle;l.bottleName=appState.bottleNames[l.nextBottle]||'Flacone';l.bottleImage=appState.bottleImages[l.nextBottle]||'';l.stage='running';l.cleanConfirmed=false;l.remainingPieces=null;event(l,`Inizio carico codice ${l.nextBottle} · ${l.bottleName} · obiettivo ${l.productionTarget.toLocaleString('it-IT')} ${unitLabel(l)}`)}
 else if(type==='reset'){const activity=l.activity,lineOwnerId=l.lineOwnerId,lineOwnerName=l.lineOwnerName;appState.lines[selected]=freshLine(selected);Object.assign(appState.lines[selected],{activity,lineOwnerId,lineOwnerName});event(appState.lines[selected],'Linea riportata a riposo')}
 await save();render()
 }catch(e){toast(e.message,true)}}
function lists(values=appState.bottles){return `<datalist id="bottles">${values.map(x=>`<option value="${esc(x)}">`).join('')}</datalist>`}
async function resizePhoto(file){return new Promise((resolve,reject)=>{const img=new Image(),reader=new FileReader();reader.onerror=reject;reader.onload=()=>{img.onload=()=>{const max=700,scale=Math.min(1,max/Math.max(img.width,img.height)),canvas=document.createElement('canvas');canvas.width=Math.round(img.width*scale);canvas.height=Math.round(img.height*scale);canvas.getContext('2d').drawImage(img,0,0,canvas.width,canvas.height);canvas.toBlob(blob=>blob?resolve(blob):reject(Error('Impossibile preparare la foto')),'image/jpeg',.76)};img.onerror=()=>reject(Error('Formato foto non leggibile'));img.src=reader.result};reader.readAsDataURL(file)})}
async function uploadBottlePhoto(file,code){const blob=await resizePhoto(file),safe=code.replace(/[^a-zA-Z0-9_-]/g,'_'),path=`${safe}.jpg`;const {error}=await db.storage.from('bottle-photos').upload(path,blob,{contentType:'image/jpeg',upsert:true});if(error)throw error;const {data}=db.storage.from('bottle-photos').getPublicUrl(path);return `${data.publicUrl}?v=${Date.now()}`}
function previewBottlePhoto(input){const f=input.files[0],p=$('#photoPreview');if(!f||!p)return;const r=new FileReader();r.onload=()=>{p.src=r.result;p.hidden=false};r.readAsDataURL(f)}
function fillBottleDetails(code){const b=code.trim(),known=appState.bottles.includes(b);if(!known)return;$('#mapName').value=appState.bottleNames[b]||'';const p=$('#photoPreview');if(appState.bottleImages[b]){p.src=appState.bottleImages[b];p.hidden=false}else{p.removeAttribute('src');p.hidden=true}}
function previewSelectedBottle(code){const b=code.trim(),p=$('#selectedBottlePreview');if(!p)return;if(!appState.bottles.includes(b)){p.innerHTML='';return}p.innerHTML=`<div class="bottle-preview-row">${appState.bottleImages[b]?`<img src="${appState.bottleImages[b]}" alt="Foto flacone">`:'<span class="no-photo mini">Foto</span>'}<span><b>${esc(appState.bottleNames[b]||'Flacone')}</b><br>${esc(b)}</span></div>`}
async function saveBottleDetails(){try{const b=$('#mapBottle').value.trim(),n=$('#mapName').value.trim(),f=$('#mapPhoto').files[0];if(!b||!n)return toast('Inserisci nome e codice',true);toast(f?'Caricamento foto in corso…':'Salvataggio in corso…');appState.bottleNames[b]=n;if(f)appState.bottleImages[b]=await uploadBottlePhoto(f,b);if(!appState.bottles.includes(b))appState.bottles.push(b);addBottleToLine(b);Object.values(appState.lines).filter(l=>l.bottle===b).forEach(l=>{l.bottleName=n;l.bottleImage=appState.bottleImages[b]||''});await save();render();toast(appState.bottleImages[b]?'Scheda e foto salvate':'Scheda salvata senza foto')}catch(e){toast(e.message||'Foto non salvata',true)}}
function loadBottleCard(code){const b=decodeURIComponent(code);$('#mapBottle').value=b;$('#mapName').value=appState.bottleNames[b]||'';const p=$('#photoPreview');if(appState.bottleImages[b]){p.src=appState.bottleImages[b];p.hidden=false}else p.hidden=true;toast('Scheda caricata')}
function selectBottleFromArchive(code){const b=decodeURIComponent(code),input=$('#bottle');if(input){input.value=b;previewSelectedBottle(b);toast('Flacone selezionato: imposta unità e obiettivo')}}
async function deleteBottle(code){const b=decodeURIComponent(code);if(!isCT())return toast('Solo il CT può eliminare i codici',true);const activeLines=Object.values(appState.lines).filter(l=>String(l.bottle).trim()===b&&l.stage!=='idle'),active=activeLines.map(l=>l.name),name=appState.bottleNames[b]||'Flacone',photo=appState.bottleImages[b]||'';const warning=active.length?`\n\nÈ in uso sulle linee ${active.join(', ')}: le linee continueranno normalmente, ma il codice sparirà dall’archivio.`:'';if(!confirm(`Eliminare il codice ${b} dall’archivio?${warning}`))return;activeLines.forEach(l=>{l.bottleName=l.bottleName||name;l.bottleImage=l.bottleImage||photo});if(!active.length){const safe=b.replace(/[^a-zA-Z0-9_-]/g,'_');await db.storage.from('bottle-photos').remove([`${safe}.jpg`])}appState.bottles=appState.bottles.filter(x=>x!==b);delete appState.bottleColors[b];delete appState.bottleNames[b];delete appState.bottleImages[b];delete appState.bottleLines[b];await save();render();toast(`Codice ${b} eliminato dall’archivio`)}
function bottleArchive(){if(!isCT())return'';const codes=archiveCodes();return `<details class="archive-details"><summary><strong>Archivio ${archiveScope==='line'?`linea ${selected}`:'di tutti i flaconi'} (${codes.length})</strong></summary><div class="scope-actions"><button class="${archiveScope==='line'?'primary':''}" onclick="setArchiveScope('line')">Linea ${selected}</button><button class="${archiveScope==='all'?'primary':''}" onclick="setArchiveScope('all')">Tutti i flaconi</button></div><div class="archive-grid">${codes.length?codes.map(b=>`<div class="archive-card">${appState.bottleImages[b]?`<img src="${appState.bottleImages[b]}" alt="">`:'<span class="no-photo">Nessuna foto</span>'}<b>${esc(appState.bottleNames[b]||'Flacone')}</b><small>${esc(b)}</small><div class="archive-actions"><button class="primary" onclick="selectBottleFromArchive('${encodeURIComponent(b)}')">Scegli e carica</button><button onclick="loadBottleCard('${encodeURIComponent(b)}')">Modifica</button><button class="danger" onclick="deleteBottle('${encodeURIComponent(b)}')">Elimina</button></div></div>`).join(''):'<p class="muted">Nessun flacone associato a questa linea.</p>'}</div></details>`}
function bottleCreateForm(){return `<strong>Crea o modifica flacone per la linea ${selected}</strong><div class="grid" style="margin-top:10px"><label class="field">Nome flacone<input id="mapName" placeholder="Es. Shampoo nero"></label><label class="field">Codice<input id="mapBottle" list="bottles" oninput="fillBottleDetails(this.value)"></label><label class="field">Foto flacone<input id="mapPhoto" type="file" accept="image/*" capture="environment" onchange="previewBottlePhoto(this)"></label></div><img id="photoPreview" class="photo-preview" hidden alt="Anteprima foto"><div class="actions"><button onclick="saveBottleDetails()">Salva nell’archivio ${selected}</button></div>`}
function colorManager(){if(!isCT())return'';const codes=archiveCodes();return `<div class="notice"><h3>Produzione Stazioni vibranti</h3><div class="archive-choice">${bottleArchive()}<label class="field archive-search">Scrivi il codice o scegli dall’archivio<input id="bottle" list="bottles" placeholder="Cerca codice" oninput="previewSelectedBottle(this.value)"></label>${lists(codes)}<div id="selectedBottlePreview"></div><div class="grid"><label class="field">Unità produzione<select id="productionUnit"><option value="pieces">Pezzi</option><option value="ot">O.T.</option></select></label><label class="field">Obiettivo da raggiungere<input id="productionTarget" type="number" min="1" placeholder="Es. 54"></label></div><div class="actions"><button class="primary" onclick="act('start-code',{bottle:document.querySelector('#bottle').value,unit:document.querySelector('#productionUnit').value,target:document.querySelector('#productionTarget').value})">Avvia sulla linea</button></div></div><hr>${bottleCreateForm()}</div>`}
function codeChanger(l){if(!isCT()||!l.bottle)return'';return `<div class="notice"><strong>CT · codice in corso: ${esc(l.bottle)}</strong><div class="grid"><label class="field">Nuovo codice<input id="changeBottle" list="bottles" placeholder="Scegli nuovo codice"></label><label class="field">Unità<select id="changeUnit"><option value="pieces">Pezzi</option><option value="ot">O.T.</option></select></label><label class="field">Nuovo obiettivo<input id="changeTarget" type="number" min="1"></label></div><div class="actions"><button onclick="act('change-code',{bottle:document.querySelector('#changeBottle').value,unit:document.querySelector('#changeUnit').value,target:document.querySelector('#changeTarget').value})">Cambia codice in corso</button></div>${lists()}</div>`}
function productionBox(l){const progress=l.productionTarget?`<h3>${Number(l.productionDone).toLocaleString('it-IT')} / ${Number(l.productionTarget).toLocaleString('it-IT')} ${unitLabel(l)}</h3><p>Mancano <strong>${productionRemaining(l).toLocaleString('it-IT')} ${unitLabel(l)}</strong></p>`:'<p>Obiettivo non ancora impostato.</p>',update=l.productionTarget&&canOperateLine(l)?`<div class="actions"><input id="productionDone" type="number" min="0" max="${l.productionTarget}" value="${l.productionDone}"><button id="updateProduction">Aggiorna quantità raggiunta</button></div>`:'',plan=isCT()?`<details><summary>CT · modifica unità o obiettivo</summary><div class="actions"><select id="planUnit"><option value="pieces" ${l.productionUnit!=='ot'?'selected':''}>Pezzi</option><option value="ot" ${l.productionUnit==='ot'?'selected':''}>O.T.</option></select><input id="planTarget" type="number" min="1" value="${l.productionTarget||''}" placeholder="Obiettivo"><button id="setProductionPlan">Salva obiettivo</button></div></details>`:'';return `<div class="notice"><strong>Avanzamento produzione</strong>${progress}${update}${plan}</div>`}
function handoverBox(l){if(!l.productionTarget)return'';return `<div class="notice"><strong>Passaggio consegne</strong>${l.handoverAt?`<p>Ultimo: ${esc(l.handoverShift)} · ${esc(l.handoverBy)} · ${showTime(l.handoverAt)}<br>${esc(l.handoverNote||'Nessuna nota')}</p>`:''}${canOperateLine(l)?`<div class="grid"><label class="field">Turno<select id="handoverShift"><option>Mattina</option><option>Pomeriggio</option><option>Notte</option></select></label><label class="field">Nota per il turno successivo<input id="handoverNote" placeholder="Situazione, problemi, materiale..."></label></div><div class="actions"><button id="saveHandover">Conferma passaggio consegne</button></div>`:''}</div>`}
function lineAssignment(l){const current=l.lineOwnerName?`Affidata a <strong>${esc(l.lineOwnerName)}</strong>`:'Nessun operatore assegnato';if(!isCT())return `<div class="notice">${current}</div>`;return `<div class="notice"><strong>Affidamento linea</strong><p>${current}</p><div class="actions"><select id="lineOwner"><option value="">Nessun operatore</option>${operators.map(o=>`<option value="${o.id}" ${o.id===l.lineOwnerId?'selected':''}>${esc(o.display_name||'Operatore')}</option>`).join('')}</select><button id="assignLine">Conferma assegnazione</button></div></div>`}
function panel(l){const undo=l.stage!=='idle'&&l.undo?'<button onclick="if(confirm(\'Annullare davvero l’ultima azione?\'))act(\'undo\')">Annulla ultima azione</button>':'',close=isCT()&&l.stage!=='idle'?'<button class="danger" onclick="if(confirm(\'Chiudere definitivamente il ciclo della linea?\'))act(\'reset\')">Chiudi ciclo</button>':'',controls=undo||close?`<div class="actions cycle-controls">${undo}${close}</div>`:'',modes=isCT()?`<div class="notice"><strong>CT · correggi modalità linea</strong><div class="actions"><button class="green" onclick="if(confirm('Impostare In corso?'))act('set-stage',{stage:'running'})">In corso</button><button class="orange" onclick="if(confirm('Impostare Pre-raclage?'))act('set-stage',{stage:'pre'})">Pre-raclage</button><button class="yellow" onclick="if(confirm('Impostare Raclage?'))act('set-stage',{stage:'raclage'})">Raclage</button><button class="blue" onclick="if(confirm('Impostare Scarico?'))act('set-stage',{stage:'unload'})">Scarico</button><button class="red" onclick="if(confirm('Impostare Finito?'))act('set-stage',{stage:'finished'})">Finito</button></div></div>`:'';const h=`<h2>Linea ${l.name}</h2><span class="status">${esc(labels[l.stage])}</span>${lineAssignment(l)}${controls}${modes}`,info=`<div class="notice"><strong>${esc(appState.bottleNames[l.bottle]||l.bottleName||'Flacone')}</strong> · Codice ${esc(l.bottle||'—')}<br>${Number(l.prePieces).toLocaleString('it-IT')} flaconi mancanti${l.extraRequested?' · 1 big bag aggiuntivo':''}</div>${productionBox(l)}${handoverBox(l)}${codeChanger(l)}`;
 if(l.stage==='idle')return h+(isCT()?colorManager():'<p>In attesa che il CT assegni il codice alla linea.</p>');
 if(l.stage==='running')return h+info+(isCT()?`<input id="bottle" type="hidden" value="${esc(l.bottle)}"><input id="extra" type="hidden" value="no"><label class="field">Flaconi mancanti<input id="prePieces" type="number" min="0" value="${Number(l.prePieces)||10000}"></label><div class="actions"><button class="primary" id="savePre">Chiama pre-raclage</button></div>`:'<p>Produzione in corso.</p>');
 if(l.stage==='pre')return h+info+`<div class="actions"><button class="primary" id="takePre">Prendo in carico</button></div>`;
 if(l.stage==='progress'&&l.remainingPieces===null){const extra=l.extraRequested?(!l.extraAssignedTo?'<button id="takeExtra">Prendo in carico big bag aggiuntivo</button>':`<p>Big bag aggiuntivo in carico a <strong>${esc(l.extraAssignedTo)}</strong>${l.extraLoaded?' · caricato':''}</p>${!l.extraLoaded&&(l.extraAssignedTo===actor()||isAdmin())?'<button id="extraLoaded">Conferma big bag caricato</button>':''}`):'';return h+info+`<p>Pre-raclage in carico a <strong>${esc(l.assignedTo)}</strong></p>${extra}<div class="grid"><label class="field">Big bag 1<select id="bag1"><option value="1">Pieno</option><option value="0">Vuoto</option></select></label><label class="field">Big bag 2<select id="bag2"><option value="1">Pieno</option><option value="0">Vuoto</option></select></label></div><div class="actions"><button class="primary" id="submitPre">Invia risposta</button></div>`}
 if(l.stage==='waiting-raclage')return h+info+(isCT()?'<div class="actions"><button class="primary" id="callRaclage">Chiama raclage</button></div>':'<p>In attesa della chiamata raclage del CT.</p>');
 if(l.stage==='raclage')return h+info+'<p>Serve la verifica dei pezzi avanzati.</p><button class="primary" id="takeRaclage">Prendo in carico la verifica</button>';
 if(l.stage==='progress'&&l.remainingPieces===-1)return h+info+`<p>Verifica in carico a <strong>${esc(l.assignedTo)}</strong></p><label class="field">Pezzi avanzati<input id="remaining" type="number" min="0"></label><div class="actions"><button class="primary" id="submitRaclage">Invia al CT</button></div>`;
 if(l.stage==='waiting-ct')return h+info+`<div class="notice"><strong>${Number(l.remainingPieces).toLocaleString('it-IT')} pezzi avanzati</strong><br>In attesa del CT.</div>${isCT()?'<div class="actions"><button class="primary" id="ctOk">CT: OK</button><button id="ctNo">CT: Non OK</button></div>':''}`;
 if(l.stage==='raclage-ok')return h+info+'<p>Raclage approvato.</p><button class="primary" id="startUnload">Avvia scarico linea</button>';
 if(l.stage==='unload')return h+info+'<p><strong>Scarico linea in corso.</strong></p><label><input id="cleanCheck" type="checkbox"> Linea completamente pulita</label><div class="actions"><button class="primary" id="confirmClean">Conferma linea pulita</button></div>';
 if(l.stage==='finished')return h+info+`<p><strong>Linea pulita confermata.</strong></p><div class="grid"><label class="field">Codice successivo<input id="nextBottle" list="bottles"></label><label class="field">Unità<select id="nextUnit"><option value="pieces">Pezzi</option><option value="ot">O.T.</option></select></label><label class="field">Obiettivo<input id="nextTarget" type="number" min="1"></label></div>${lists()}<div class="actions"><button class="primary" id="nextLoad">Conferma inizio carico</button></div>`;
 return h+info+'<p>Carico in corso.</p>'}
function bind(){const on=(id,fn)=>{const e=$('#'+id);if(e)e.onclick=fn};on('assignLine',()=>act('assign-line',{ownerId:$('#lineOwner').value}));on('setProductionPlan',()=>act('set-production-plan',{unit:$('#planUnit').value,target:$('#planTarget').value}));on('updateProduction',()=>act('update-production',{value:$('#productionDone').value}));on('saveHandover',()=>act('handover',{shift:$('#handoverShift').value,note:$('#handoverNote').value}));on('saveColor',async()=>{const b=$('#mapBottle').value.trim(),c=$('#mapColor').value.trim();if(!b||!c)return toast('Inserisci codice e colore',true);appState.bottleColors[b]=c;if(!appState.bottles.includes(b))appState.bottles.push(b);await save();render();toast('Associazione salvata')});on('savePre',()=>act('save-pre',{bottle:$('#bottle').value,prePieces:$('#prePieces').value,extraRequested:$('#extra').value==='yes'}));on('takePre',()=>act('take-pre'));on('takeExtra',()=>act('take-extra'));on('extraLoaded',()=>act('extra-loaded'));on('submitPre',()=>act('submit-pre',{bag1:$('#bag1').value,bag2:$('#bag2').value}));on('callRaclage',()=>act('call-raclage'));on('takeRaclage',()=>act('take-raclage'));on('submitRaclage',()=>act('submit-raclage',{remainingPieces:$('#remaining').value}));on('ctOk',()=>act('ct-decision',{decision:'ok'}));on('ctNo',()=>act('ct-decision',{decision:'no'}));on('startUnload',()=>act('start-unload'));on('confirmClean',()=>$('#cleanCheck').checked?act('confirm-clean'):toast('Conferma la verifica della linea',true));on('nextLoad',()=>act('next-load',{nextBottle:$('#nextBottle').value,unit:$('#nextUnit').value,target:$('#nextTarget').value}));on('reset',()=>act('reset'))}
function goToBottleArea(mode){const panel=$('#panel');if(!panel.querySelector('.archive-details')||!$('#mapBottle')){const tools=document.createElement('div');tools.id='quickBottleTools';tools.className='notice quick-bottle-tools';tools.innerHTML=`<h3>Produzione Stazioni vibranti · Linea ${esc(selected)}</h3>${bottleArchive()}<hr>${bottleCreateForm()}`;panel.prepend(tools)}if(mode==='archive'){const archive=panel.querySelector('.archive-details');archive.open=true;archive.scrollIntoView({behavior:'smooth',block:'start'});setTimeout(()=>archive.querySelector('summary')?.focus(),350)}else{const input=$('#mapBottle');input.scrollIntoView({behavior:'smooth',block:'center'});setTimeout(()=>input.focus(),350)}}
function showLineQuickMenu(line){selected=line;render();loadHistory();const old=$('#lineQuickMenu');if(old)old.remove();const dialog=document.createElement('dialog');dialog.id='lineQuickMenu';dialog.className='line-quick-dialog';dialog.innerHTML=`<button class="line-quick-close" aria-label="Chiudi">×</button><strong>Linea ${esc(line)}</strong><p>Cosa vuoi fare?</p><div class="line-quick-actions"><button class="primary" data-go="create">Crea flacone</button><button data-go="archive">Archivio flaconi</button></div>`;document.body.appendChild(dialog);dialog.querySelector('.line-quick-close').onclick=()=>dialog.close();dialog.querySelectorAll('[data-go]').forEach(button=>button.onclick=()=>{const mode=button.dataset.go;dialog.close();goToBottleArea(mode)});dialog.onclose=()=>dialog.remove();dialog.showModal()}
function render(){if(profile?.status!=='approved')return accessScreen();$('#access').innerHTML='';$('#app').hidden=false;$('#identity').innerHTML=`<span><strong>${esc(actor())}</strong><br><small>${profile.role==='admin'?'Amministratore':profile.role==='ct'?'CT':'Operatore'}</small><br><small id="syncStatus">Connessione in corso…</small></span><button id="notifications">${notificationAllowed()?'Notifiche attive':'Attiva notifiche'}</button>${isCT()?'<button id="exportHistory">Esporta storico</button>':''}<button id="changePassword">Cambia password</button><button id="logout">Esci</button>`;$('#notifications').onclick=enableNotifications;if($('#exportHistory'))$('#exportHistory').onclick=exportHistory;$('#changePassword').onclick=showPasswordChange;$('#logout').onclick=signOut;let passwordPanel=$('#passwordPanel');if(!passwordPanel){passwordPanel=document.createElement('section');passwordPanel.id='passwordPanel';passwordPanel.className='access-card';passwordPanel.hidden=true;passwordPanel.innerHTML='<h2>Crea o cambia password</h2><div class="grid"><label class="field">Nuova password<input id="insidePassword" type="password" minlength="8" autocomplete="new-password"></label><label class="field">Ripeti nuova password<input id="insidePasswordConfirm" type="password" minlength="8" autocomplete="new-password"></label></div><div class="actions"><button class="primary" id="saveInsidePassword">Salva password</button><button id="cancelInsidePassword">Annulla</button></div>';document.body.insertBefore(passwordPanel,$('#app'));$('#saveInsidePassword').onclick=changePasswordInside;$('#cancelInsidePassword').onclick=()=>passwordPanel.hidden=true}$('#lines').innerHTML=Object.values(appState.lines).map(l=>{const name=appState.bottleNames[l.bottle]||l.bottleName||'Flacone',photo=appState.bottleImages[l.bottle]||l.bottleImage||'',remaining=l.productionTarget?productionRemaining(l):0;return `<button class="line ${cls(l.stage)} ${l.name===selected?'selected':''}" data-line="${l.name}"><strong class="line-code">${l.name}</strong>${l.bottle?`<div class="line-bottle-name">${esc(name)}</div><div class="line-image">${photo?`<img class="line-photo" src="${photo}" alt="Foto flacone">`:'<span class="no-photo">Foto non salvata</span>'}</div>`:'<div class="line-empty">Nessun flacone in corso</div>'}<div class="line-card-footer"><b>${esc(labels[l.stage])}</b>${l.productionTarget?`<span>${Number(remaining).toLocaleString('it-IT')} ${unitLabel(l)} da fare</span>`:''}</div>${l.lineOwnerName?`<small class="line-operator">Operatore: ${esc(l.lineOwnerName)}</small>`:''}</button>`}).join('');document.querySelectorAll('.line').forEach(b=>b.onclick=()=>isCT()?showLineQuickMenu(b.dataset.line):(selected=b.dataset.line,render(),loadHistory()));const l=appState.lines[selected];$('#panel').innerHTML=panel(l);bind();$('#activity').innerHTML=l.activity.length?l.activity.map(e=>`<div class="event ${esc(displayActivityKind(e))}"><strong>${esc(e.text)}</strong><small>${esc(e.actor)} · ${showTime(e.at)}</small></div>`).join(''):'<p class="muted">Nessuna attività.</p>';loadHistory();if(isAdmin())renderAdmin()}
function adminUserRow(u,statusLabel){const search=`${u.display_name||''} ${u.username||''} ${u.email||''}`.toLowerCase();return `<div class="user-row" data-user-search="${esc(search)}"><div><strong>${esc(u.display_name||'Senza nome')}</strong><small>@${esc(u.username||'—')} · ${esc(u.email)}</small></div><select data-role="${u.id}" ${u.role==='admin'?'disabled':''}><option value="operator" ${u.role==='operator'?'selected':''}>Operatore</option><option value="ct" ${u.role==='ct'?'selected':''}>CT</option>${u.role==='admin'?'<option value="admin" selected>Amministratore supremo</option>':''}</select><span class="badge ${u.status}">${statusLabel[u.status]||u.status}</span>${u.role==='admin'?'':`<button data-status="${u.id}" data-next="${u.status==='approved'?'blocked':'approved'}">${u.status==='approved'?'Rimuovi accesso':u.status==='blocked'?'Riattiva':'Approva'}</button>`}</div>`}
function filterUserArchive(value){const query=value.trim().toLowerCase();document.querySelectorAll('#userArchiveList .user-row').forEach(row=>row.hidden=!row.dataset.userSearch.includes(query));const visible=[...document.querySelectorAll('#userArchiveList .user-row')].filter(row=>!row.hidden).length;const count=$('#archiveResultCount');if(count)count.textContent=query?`${visible} risultati`:''}
async function renderAdmin(){let box=$('#adminPanel');if(!box){box=document.createElement('section');box.id='adminPanel';box.className='admin';document.body.insertBefore(box,$('#app'))}const {data,error}=await db.from('profiles').select('*').order('created_at',{ascending:false});if(error)return;const statusLabel={pending:'In attesa',approved:'Attivo',blocked:'Bloccato'},pending=data.filter(u=>u.status==='pending'),archived=data.filter(u=>u.status!=='pending');box.innerHTML=`<h2>Amministratore supremo</h2><p>Scegli CT oppure Operatore, poi approva. Dopo l’approvazione la persona passa automaticamente nell’archivio.</p><section class="pending-users"><h3>Nuove richieste <span class="admin-count">${pending.length}</span></h3>${pending.length?pending.map(u=>adminUserRow(u,statusLabel)).join(''):'<p class="muted">Nessuna richiesta in attesa.</p>'}</section><details class="user-archive"><summary><strong>Archivio utenti</strong> <span class="admin-count">${archived.length}</span></summary><label class="field user-search">Cerca persona<input type="search" placeholder="Nome, username o e-mail" oninput="filterUserArchive(this.value)"></label><small id="archiveResultCount" class="muted"></small><div id="userArchiveList">${archived.map(u=>adminUserRow(u,statusLabel)).join('')}</div></details>`;box.querySelectorAll('[data-role]').forEach(e=>e.onchange=()=>updateUser(e.dataset.role,{role:e.value}));box.querySelectorAll('[data-status]').forEach(e=>e.onclick=()=>updateUser(e.dataset.status,{status:e.dataset.next}))}
async function updateUser(id,change){const {error}=await db.from('profiles').update(change).eq('id',id);if(error)return toast(error.message,true);toast('Utente aggiornato');renderAdmin()}
async function boot(){if(DEMO_MODE){profile={display_name:'CT Demo',role:'ct',status:'approved'};render();await loadRemote();return}const {data}=await db.auth.getSession();session=data.session;if(session){try{profile=await getProfile()}catch(e){toast('Profilo non disponibile',true)}}if(IS_RECOVERY&&session)accessScreen();else if(profile?.status==='approved'){await loadOperators();render();await loadRemote();startRealtime()}else accessScreen();db.auth.onAuthStateChange(async(event,s)=>{session=s;if(s){profile=await getProfile();if(event==='PASSWORD_RECOVERY')accessScreen();else if(profile.status==='approved'){await loadOperators();render();loadRemote();startRealtime()}else accessScreen()}else accessScreen()})}
if('serviceWorker'in navigator)navigator.serviceWorker.register('./service-worker.js').catch(()=>{});boot();setInterval(async()=>{if(session){try{const p=await getProfile();if(p.status!==profile?.status||p.role!==profile?.role){profile=p;render()}}catch{}}loadRemote()},2500);
